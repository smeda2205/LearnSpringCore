This example explains the usage of ApplicationContext.

ApplicationContext does all the functionalities a BeanFactory does. On top of that, it does many 
additional functionalities like Event notifications, AOP, etc.

We need to use BeanFactory only when the resources are very crucial.

